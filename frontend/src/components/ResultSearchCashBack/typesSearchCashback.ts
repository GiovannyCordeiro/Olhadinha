import { Params } from "react-router-dom"

export interface ParamsProp {
    params: Params,
}

export interface DataRootAPI {
    cuponomia: string;
    intershop: string;
    meudimdim: string;
    zoom: string;
}